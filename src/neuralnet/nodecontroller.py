from abc import ABCMeta, abstractmethod
from .node import AbstractNode, SimpleNode
from operator import add, mul
import numpy as np


class NetworkGraph():
    graph_matrix = 
    def make_node_list(self, number, node_type=SimpleNode, node_list=[]):
        new_list = mul(list(SimpleNode), number)
        return add(node_list, new_list)

    def make_graph(self, node_number, edge_number, node_list, edge_list):

class Layer(NetworkGraph):
    def make_layer(self):
        pass
